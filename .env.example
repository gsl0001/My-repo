# Copy to .env (gitignored) and fill in for local/live use. NEVER commit real secrets.
# Phase 3 (live) credentials — leave blank until you wire the live broker yourself.
TRADEZERO_API_KEY=
TRADEZERO_API_SECRET=
POLYGON_API_KEY=
IBKR_ACCOUNT=
